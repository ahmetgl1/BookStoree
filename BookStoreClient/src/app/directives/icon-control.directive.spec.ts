import { IconControlDirective } from './icon-control.directive';

describe('IconControlDirective', () => {
  it('should create an instance', () => {
    const directive = new IconControlDirective();
    expect(directive).toBeTruthy();
  });
});
